<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8" />
		<meta http-equiv="x-ua-compatible" content="ie=edge,chrome=1">
		<% if (mode === 'prod') { %>
			<%- `@Html.Action("SeoModel","PartialView",new { name = "seoIndex", id = 1})` %>
		<% }else{ %>
			<%- `<title></title>` %>
		<% } %>
		<script type="text/javascript" src="/js/jquery.min.js"></script>
	</head>
	<body>
		<!-- build:header -->
		<% if (mode === 'prod') { %>
			<%- `@Html.Partial("header")` %>
		<% }else{ %>
			<%- include header %>
		<% } %>
		<!-- enbuild -->
		<div>
			
		</div>
		<!-- build:footer -->
		<% if (mode === 'prod') { %>
			<%- `@Html.Partial("footer")` %>
		<% }else{ %>
			<%- include footer %>
		<% } %>
		<!-- enbuild -->
	</body>
</html>